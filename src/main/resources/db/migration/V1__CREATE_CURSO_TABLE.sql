CREATE TABLE curso (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR2(100) NOT NULL,
    descricao CLOB NOT NULL,
    instrutor VARCHAR2(100) NOT NULL,
    duracao NUMBER NOT NULL
);